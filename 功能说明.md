# 护照边界处理系统 - 功能说明

本系统是一个用于护照 PDF 处理、边界候选生成及人工校对标注的工具。它结合了传统计算机视觉（CV）算法与大模型（Qwen AI）技术，提供边界候选供人工确认；系统遵循“宁可人工也不误裁”的原则，不会在未确认的情况下自动输出最终结果。

## 1. 核心功能

### 前端功能 (Vite + React + Ant Design)

- **PDF 上传**: 支持拖拽上传护照 PDF 文件。
- **页面列表管理**: 上传后的 PDF 会被拆分为单页，并以双列卡片形式展示缩略图。
- **交互式审核界面**:
  - **自动候选展示**: 系统会展示边界候选方案：**候选1 (绿色)**、**候选2 (蓝色)**（取决于后端是否启用 Qwen，候选数量可能为 1 或 2）。
  - **一键采用**: 点击按钮即可快速采用 AI 或 CV 推荐的边界方案。
  - **手动标注**: 若候选方案不准确，用户可以通过点击图片四个角进行精确的手动 4 点标注（红色显示）。
  - **状态反馈**: 页面卡片会显示“待处理”或“已标注”状态。

### 后端功能 (FastAPI + Python)

- **PDF 拆分**: 使用 `PyMuPDF` 高效地将 PDF 转换为高质量 PNG 图片。
- **按需 AI 检测**: 只有在用户打开特定页面进行审核时，系统才会调用 Qwen 大模型（若配置了 API Key），以节省成本和提高响应速度。
- **CV 候选生成**: 始终包含基于 OpenCV 的基础边界检测方案。
- **标注存储**: 保存用户确认的边界数据，并自动生成带红框叠加的遮罩图片（Mask）。

## 2. 用户操作流程

1. **上传文件**: 进入首页，将护照 PDF 文件拖入上传区域。
2. **浏览页面**: 上传完成后，系统会自动跳转到页面列表，展示 PDF 的每一页。
3. **进入审核**: 点击任意页面卡片，进入详细的审核标注界面。
4. **选择/标注边界**:
   - 如果**绿色**或**蓝色**框线已经准确覆盖护照，点击对应的“采用候选”按钮。
   - 如果框线不准确，请依次点击护照的四个角（左上、右上、右下、左下），形成**红色**标注框。
5. **提交保存**: 点击“提交手动标注”或采用按钮，系统会保存结果并返回列表页。
6. **核对状态**: 已标注的页面会显示绿色的“已标注”标签。

## 3. 本地开发与运行

### 环境要求

- **Python**: 3.11+
- **Node.js**: 18+

### 快速启动

1. **安装依赖**:

   ```bash
   # 安装后端依赖
   pip install -r requirements.txt -r backend/requirements.txt
   
   # 安装前端依赖
   cd frontend
   npm install
   ```

2. **配置环境变量**:
   在项目根目录创建 `.env` 文件，并填写您的 DashScope API Key（用于启用 Qwen AI 检测）：

   ```env
   DASHSCOPE_API_KEY=您的密钥
   ```

3. **启动后端 (端口 8000)**:

   ```bash
   # 在根目录下运行
   uvicorn backend.main:app --host 127.0.0.1 --port 8000 --reload
   ```

4. **启动前端 (端口 5173)**:

   ```bash
   cd frontend
   npm run dev
   ```

   访问 `http://localhost:5173` 开始使用。

## 4. 输出与存储

所有处理后的数据均存储在 `backend/data/runs/` 目录下，按 `run_id` 分组：

- `input.pdf`: 原始上传文件。
- `pages/`: 拆分后的 PNG 图片及页面元数据（`.json`）。
- `masks/`: 用户标注后生成的带红框叠加的最终图片。

## 5. 常见问题 (FAQ)

- **Q: 为什么没有看到候选框？**
  - A: 请确保后端已正确启动，并且检查是否在 `.env` 中配置了 `DASHSCOPE_API_KEY`。如果未配置，将只有 CV 候选方案。
- **Q: 为什么前端无法连接到后端？**
  - A: 默认前端代理配置为 `http://127.0.0.1:8000`。请确保后端在该地址和端口运行。
- **Q: 自动检测不准确怎么办？**
  - A: 本系统遵循“人工确认优先”原则。自动检测仅作为参考，用户应始终进行人工核对，并在必要时进行手动 4 点标注。
- **Q: 是否支持图片旋转？**
  - A: 目前版本尚未实现界面旋转控制，请确保上传的 PDF 页面方向基本正确。
